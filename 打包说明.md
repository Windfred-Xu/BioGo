# 生物棋打包说明

## Windows 安装包（MSI）

在项目目录执行：

```powershell
npm install
npm run build:msi
```

产物位于 `dist`，为 64 位 MSI 安装包。安装时会创建桌面和开始菜单快捷方式。

## Android 安装包（APK）

首次准备：

```powershell
npm install
node node_modules/@capacitor/cli/bin/capacitor add android
npm run android:sync
```

调试版 APK：

```powershell
npm run android:debug
```

产物：`android\app\build\outputs\apk\debug\app-debug.apk`。

发布版 APK：

```powershell
npm run android:release
```

Android 构建需要 JDK 21、Android SDK 和已接受的 SDK 许可证。发布到商店前还应配置自己的签名密钥；未配置时 Gradle 只能生成调试版或未签名发布包。

## 资源同步

`npm run prepare:mobile` 会重新生成 `www`，其中包含游戏页面、脚本、`vendor` 依赖及全部 `bgm` 音乐。每次修改游戏或音乐后，在构建 APK 前运行同步命令即可。
