# 生物棋 · v0.1

双击 `index.html` 进入游戏。不覆盖外面的“奇偶围棋”，此目录为独立适配版。

## 已接入

三派系互斥选择（每方首次落子确定且整局固定），病毒替换、细菌归群迁移、真菌自由迁移。原有六档 AI、人机 / AI 对弈、主题编辑、计时、局面编辑、悔棋、结束结算、棋谱导入导出、复盘与联机框架继续使用。纯围棋模式保留原奇偶围棋规则。

UI 使用派系图标和色环、迁移目标高亮及复盘派系数量图。新对局不使用旧版 VP 与等级机制。

## 规则落地约定

- 以奇偶围棋为基础，迁移不消耗被提方正常回合；完成提子效果后轮到被提方落子。每次落子、提子与迁移效果结束后，先全盘复查并清除所有零气棋群，稳定后才换手或进行终局判定。双方连续停手、无合法落子、满盘、认输或和棋均可结束。
- 三派系共用提子次数规则：同一棋子累计被提两次后，第 3 次被提不再发动任何迁移或替换，直接剔除。未达到第 3 次时，病毒被提会自动将距离被提位置最短的提子方相连棋子替换为该病毒。
- 真菌被提后（第 1、2 次），由被提方点击高亮位置，从提子方所有气孔中自由指定迁移点，不受最短路径限制；多个被提真菌逐个选择。不再产生孢子。
- 每次迁移后重新检查可存活气孔，不能使本次已迁移棋子失去全部气。剩余棋子无可用气孔时自动剔除并结束迁移阶段，不再等待用户选点；AI 对弈也会自动处理。
- 细菌被提后（第 1、2 次），迁到本次提子后仍在盘上的其他己方细菌的气孔；不以本次新迁入的棋子扩展候选范围。由被提方玩家或 AI 自由选择合法气孔，不限距离。迁入棋子、本批已迁入棋子和原有菌群均须有气；没有合法点则剔除。
- 旧棋谱可能包含已取消的孢子或旧迁移状态，建议重开新局使用修订规则；旧实验数据保留，不表示新规则结果。
- 新对局完全移除 VP、等级、有限牌库、感染、生物膜、二分裂、远播和孢子复苏。

## 联机

在此目录运行 `node server.cjs`，打开 `http://localhost:8766`。同一局域网其他设备使用终端显示的局域网地址，房主创建房间、对手输入房间码。须允许系统防火墙访问该端口。公共服务器选项依赖第三方服务可用性，不保证不同网络均可连接；房间码仅用于约局，不作为安全认证。

## 验证

生物棋 AI 六档使用固定节点预算，最高档为最多 4 层行动搜索、2400 节点、候选宽度 5 的选择性极小极大搜索。迁移选点也进入同一搜索流程。预算耗尽时采用上一轮完整搜索结果；相同状态与种子产生相同决策。它不是穷举求解器，最高难度自对弈胜率不能等同于人类高手对局的理论平衡。

`node migration-v3.test.cjs`：现行迁移、无菌群剔除、细菌不限距离选点、真菌自由选点、耗尽与内嵌同步回归。

`node migration-v3-browser.test.cjs`：真菌高亮点击及迁移后回合推进的页面回归。

`node balance-v3.cjs`：每组沿用旧实验前 25 对种子并交换黑白，共 150 盘，最高档对弈；输出到 `balance-v3-results/`，不覆盖旧实验。

`npm test`：设计方模块测试与整合规则回归。

`node bio-browser.test.cjs`：本机 Edge 自动化交互测试（脚本使用当前开发环境的 Playwright 路径）。

原 Chemiss 基线保留其许可证，旧化学规则脚本作为不执行的基线文本保留。实际运行的生物引擎由原围棋引擎、设计方模块和 `bio-engine.js` 组成；HTML 内嵌同一份组合引擎供离线页面及 AI Worker 使用。引擎修改时须同步内嵌版本。
